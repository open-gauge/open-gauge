# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# fumadocs-mdx
/.source

# generated OpenAPI content (fetched/generated by scripts/*.mjs before dev/build)
# NOTE: openapi.json itself is NOT ignored — it's committed as a fallback snapshot for
# hosts that build this site without a reachable API (see scripts/fetch-openapi.mjs).
/content/docs/api/**

# misc
.DS_Store
*.log
